# V30 Addendum

V30 adds a centralized **Output Center** tab to the Image Studio desktop application.

Workflow: choose artwork → choose output root → choose Spot/CMYK → configure DPI/underbase/halftone → Build Export Package.

The exporter creates a job folder with master artwork, channel PNGs, optional underbase/halftones, contact sheet, manifest and optional ZIP package.
