# V41 Addendum
V41 adds a local archive/version layer. It intentionally uses file snapshots and SHA-256 manifests rather than proprietary version-control or binary diffing. Restore copies the snapshot source files to a user-selected folder and never overwrites silently.
