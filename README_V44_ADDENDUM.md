# V44 Addendum

V44 adds the Smart Workflow tab. Choose an artwork and output folder, set target DPI/max side/cleanup strength, then run the workflow. Each stage produces inspectable files and a JSON manifest; the final folder includes `production_package.zip`.
