# V48 Addendum

V48 adds a Full System Validation & Test Center so the application can report which layers are present, which optional dependencies are installed, and whether core image/database/archive workflows pass deterministic smoke tests.
