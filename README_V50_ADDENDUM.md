# V50 Addendum
V50 adds a performance diagnostics layer for large T-shirt artwork. It estimates memory usage, recommends a conservative tile size from an available-memory budget, creates optimized previews, and reports optional compute backends. It is diagnostic/optimization support, not a promise of GPU acceleration.
