# V53 Addendum
V53 adds Profile & Workspace management. It intentionally provides a local activation framework rather than pretending to implement a cloud license server. Workspace bundles are integrity-checked with SHA-256 before restore.
