# V53 Roadmap — Profile, Local License & Workspace Bundles
- Persistent user/shop profile.
- Offline local activation framework with checksum validation; not a remote licensing service.
- Export/verify/restore workspace bundle with SHA-256 manifest.
- Includes V52 settings/presets and available local job databases.
- Restore is non-destructive by default (existing files are skipped).
