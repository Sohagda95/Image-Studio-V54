# Image Studio V8

## Added
- Optional AI Cutout integration through `rembg` + ONNX Runtime.
- `.isp` project container for saving production settings/preview.
- Production-report helper for separation metadata.
- Dedicated **V8 Production** menu.
- Graceful fallback when optional AI dependencies are not installed.

## Important
The existing separation engine remains an engineering/preview workflow, not a certified RIP.
Validate final separations on the target production setup.
